export default class SafeString {
  private string: string;

  constructor(string: string) {
    this.string = string;
  }

  toString(): string {
    return this.toHTML();
  }

  toHTML(): string {
    return '' + this.string;
  }
}